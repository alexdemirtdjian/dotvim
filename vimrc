execute pathogen#infect()
syntax on
colorscheme molokai
filetype plugin indent on


" Tab settings
" shiftwitth : amount of white space using indentation > and <
" softabstop : a tab is created each 4 white spaces
" tabstop : width of a tab if present 
" expandtab : insert spaces instead of tabs
:set tabstop=4 softtabstop=4 shiftwidth=4 expandtab

" show line number
:set nu

" enable the mouse
:set mouse=a
